import React from 'react';
import ReactDOM from 'react-dom/client';
import ThemeColorScreenshot from './ThemeColorScreenshot';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <ThemeColorScreenshot />
  </React.StrictMode>
);
